// examples/demo.ltl — runnable demo for ltl_kml_read

import std::io
use ltl_kml_read as lib

fn main() {
    io::println("ltl_kml_read demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}